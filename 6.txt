def gcd (a, b):
    если a % b = = 0:
        возвращение b
    возврат gcd(b, A % b)
def gcdn():
    num = [6, 12, 18]
    g = gcd(num[0], num[1])
    для i в диапазоне(2, len(num)):
        g = gcd(g, число[i])
    print(f'НОД чисел в списке равен: {g}')
    возврат g;
def lcm(k,g):
    print(f'НОК чисел в списке равен: {k/g}')


a = gcdn()
k = 0
num = [6, 12, 18]
для i в диапазоне(0, len(num)):
    k += num[i]
lcm(k,a);